const nombre = "Eduardo";
const apellido = "Gonzalez";
const dos_min = new Date(2023, 0, 20, 17, 40);

const miObjeto = {
    nombre: nombre,
    apellido: apellido,
};

// localStorage.setItem("miObjeto", JSON.stringify(miObjeto));
// sessionStorage.setItem("miObjeto", JSON.stringify(miObjeto));
// document.cookie = "CookieEduardo = SoyUnaCookie; expires=" + dos_min;
